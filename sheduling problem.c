#include<stdio.h>
#include<limits.h>
int main()
{
    int i,minfin,t,a[1000][2],j,n;
    scanf("%d",&n);
    scanf("%d",&t);

    minfin=INT_MAX;

    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i][0],&a[i][1]);
        if(a[i][1]<minfin)
            minfin=a[i][1];
    }

    for(i=0;i<)
    {
        if(a[i][1]<minfin)
            minfin=a[i][1];
    }

    while(i<t)
    {

    }

return 0;
}
