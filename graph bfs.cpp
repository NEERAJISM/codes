#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t,n,s,d,i,j,initial;
    vector<vector<int>> myadjlist(6);
    cin>>n;
    vector<bool> boolean(n);

    for(i=0;i<n;i++)
    {
    	scanf("%d%d",&s,&d);
    	myadjlist[s].push_back(d);
    }

    cin>>initial;

    queue<int> q;





return 0;
}

