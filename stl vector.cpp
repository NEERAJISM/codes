#include <bits/stdc++.h>
using namespace std;
int main()
{
    deque<int> s;

    s.push_front(5);
    s.push_back(4);

    cout<<s.front();

    //cout<<s.remove_front();
    //cout<<s.front();


    //cout<<s.(4);


	/*v.push_back(10);
	v.insert(v.begin(),9);
	v.insert(v.end(),11);
	//v.insert(v.begin(),8);
	v.pop_back();
	v.delete();

	int n;
	//cout<<"enter the no. of elements"<<endl;
	//cin>>n;

	v.insert(v.begin(),10);
	v.insert(v.begin(),5);
	v.insert(v.begin(),12);
	v.insert(v.begin(),13);

	//cout<<v.empty();
*/
   /* vector<int>::iterator i = v2.begin();
    //sort(v.begin(),v.end());

	for (i; i!=v2.end(); i++)
	{
		cout << *i <<" ";
	}*/
	return 0;
}
