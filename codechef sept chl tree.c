#include<stdio.h>
#include<string.h>
#include<math.h>
int main()
{
    int T,b;
    scanf("%d",&T);
    for(b=0;b<T,b++)
    {
        int level,c,l,q;
        char a[100000];
        gets(a);
        q=strlen(a);
        int *A=(int *)malloc(q*sizeof(int));
        for(d=0;d<q;d++)
        {


        }
        level=q+1;
        if(level%2==0)
        {
            l=level/2;
            c=

        }
    }
}
