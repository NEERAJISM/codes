#include <iostream>
#include <cstdio>
using namespace std;
char c[51][51];
queue< pair<int ,int> > q;

int findall(int  x,int y,int val,int ch)
{

}

int main()
{
    int h,w,i,j;
    while(1)
    {
        scanf("%d%d",&h&w);

        if(h==0 && w==0)
            break;

        for(i=1;i<=h;i++)
            for(j=1;j<=w;j++)
                cin>>c[i][j];

        for(i=1;i<=h;i++)
        {
            for(j=1;j<=w;j++)
            {
                if(c[i][j]=='A')
                {
                    lenght =1;
                    q.push(make_pair(i,j));

                    x=i;
                    y=j;

                    while(q.empty()==0)
                    {
                        if((int) c[x+1][y] = ch+1)
                        {
                            q.
                        }

                        if((int) c[x+1][y] = ch+1)
                        {

                        }

                        if((int) c[x+1][y] = ch+1)
                        {

                        }

                        if((int) c[x+1][y] = ch+1)
                        {

                        }

                        if((int) c[x+1][y] = ch+1)
                        {

                        }

                        if((int) c[x+1][y] = ch+1)
                        {

                        }

                        if((int) c[x+1][y] = ch+1)
                        {

                        }

                        if((int) c[x+1][y] = ch+1)
                        {

                        }
                    }
                }
            }
        }
    }
}
