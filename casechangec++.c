#include<bits/stdc++.h>

using namespace std;
int main()
{
    string c;
    int t=0;

    while(c[i]!='\0')
    {
        if(c[i].isupper()==1)
            c[i].tolower();
    }

    cout<<c;
return 0;
}
